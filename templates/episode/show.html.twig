{% extends 'base.html.twig' %}

{% block title %}Episode{% endblock %}

{% block body %}
    <h1 class="color-yell">Episode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th class="color-yell">Id</th>
                <td class="color-whrite">{{ episode.id }}</td>
            </tr>
            <tr>
                <th class="color-yell">Title</th>
                <td class="color-whrite">{{ episode.title }}</td>
            </tr>
            <tr>
                <th class="color-yell">Summary</th>
                <td class="color-whrite">{{ episode.summary }}</td>
            </tr>
        </tbody>
    </table>

    <a class="color-yell" href="{{ path('episode_index') }}">back to list</a>

    <a class="color-yell" href="{{ path('episode_edit', {'id': episode.id}) }}">edit</a>

    {{ include('episode/_delete_form.html.twig') }}
{% endblock %}
